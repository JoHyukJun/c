/*
    - a 가 요소 개수 n 이고 요소형이 Type 형인 배열일 떄 식 a 는 a[(1)] 을 가리키는 포인터다.
    - Type * 형 포인터 p 가 배열 a 안의 요소 a[k] 를 가리킨다고 하자. 이때 p + i 는 a[(2)] 를 가리키고 p - i 는 a[(3)] 를 가리킨다.
    - p 를 인크리먼트하면 p 는 a[(4)] 를 가리키도록 변경된다.
    - (1): 0
    - (2): k + i
    - (3): k - i
    - (4): k + 1 
*/


#include <stdio.h>


int main(void) {

    return 0;
}