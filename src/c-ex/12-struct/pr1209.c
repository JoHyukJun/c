/*
    - 다음은 x, y, z 라는 3개의 (1) 를 가진 구조체 XYZ 라는 자료형 이름을 부여하는 선인이다.
    - 다음 문제 이후의 XYZ 형은 이 문제에서 선언한 것을 의미한다.
    - (1): 멤버 변수
*/


#include <stdio.h>


typedef struct {
    int x;
    long y;
    double z;
} XYZ;


int main(void) {

    return 0;
}