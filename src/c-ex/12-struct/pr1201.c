/*
    - 다음은 3개의 (1) 를 가진 구조체 선언이다.
    - 여기서 xyz 는 (2) 라고 하며 이 구조체에 부여된 이름이다.
    - 또한 a 와 b 는 이 구조체의 자료형을 가진 실체, 즉 객체다.
    - 다음 문제 이후 구조체 xyz 는 이 문제에서 선언한 것을 의미한다.
    - (1): 멤버 변수
    - (2): 구조체 태그
*/


#include <stdio.h>


struct xyz {
    int x;
    long y;
    double z;
} a, b;


int main(void) {

    return 0;
}