/*
    - 배열은 같은 자료형의 요소를 직선 위에 나열한 데이터 구조다. 배열 안 임의의 요소를 참조하기 위한 연산자 [] 의 이름은 (1) 연산자다.
    - 이 연산자를 적용한 식 a[i] 는 배열 a 앞쪽에서 (2) 개 뒤의 요소에 접근한다.
    - 요소 위치를 지정하기 위한 [] 안의 정숫값을 (3) 라고 한다.
    - (1): 첨자(subscript)
    - (2): i
    - (3): 첨자
*/


#include <stdio.h>


int main(void) {
    return 0;
}