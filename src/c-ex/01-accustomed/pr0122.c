/*
    - pr0121.c 에서 (상수) 임에 대해 n1, n2, c, t 는 값을 자유롭게 넣고 뺄 수 있는 (변수) 이다. point1은 각 (변수) 의 자료형과 이름을 부여하기 위한 (선언) 이다. 덧붙여 c의 (선언) 에서 = 오른쪽에 있는 3은 c에 넣을 값을 지정하는 것으로, (초기화 값) 이라고 한다.
*/


#include <stdio.h>


int main(void) {
    return 0;
}