/*
    - 화면에 출력하는 (함수) 로는 printf나 puts가 있다. printf에 전달하는 첫 번째 (인수)는 (형식 문자열)이라고 하며 그 안에는 계속해서 (형식 문자열)의 출력 형식을 지정하기 위한 (형식 지정자)를 포함할 수 있다. 그리고 정숫값을 10진수로 출력하기 위한 (형식 지정자)는 (%d) 이다. 또한, printf를 이용한 출력에서는 출력 마지막에 자동으로 줄 바꿈이 (없다). puts를 이용했을 때는 출력 마지막에 자동으로 줄 바꿈이 (있다).
*/


#include <stdio.h>


int main(void) {
    return 0;
}