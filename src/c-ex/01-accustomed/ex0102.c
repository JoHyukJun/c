#include <stdio.h>


/*
    - printf 함수에 첫 번째 인수는 format string 이라고 함
    - format string 안의 %d 는 형식을 지시하는 conversion specification 이라 함

*/
int main(void) {
    printf("%d\n", 11 - 38);

    return 0;
}