/*
    - C 언어의 프로그램에서는 표준 스트림과 연결된 FILE * 형 변수를 제공한다.
    - 표준 입력 스트림과 연결된 것이 (1), 표준 출력 스트림과 연결된 것이 (2), 표준 오류 스트림과 연결된 것이 (3) 이다.
    - (1): stdin
    - (2): stdout
    - (3): stderr
*/


#include <stdio.h>


int main(void) {

    return 0;
}