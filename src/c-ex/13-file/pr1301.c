/*
    - C 언어 프로그램에서는 (1), (2), (3) 이라는 3가지 표준 스트림이 준비돼 있다.
    - 이들 스트림을 가리키는 포인터는 (4) 헤더 안에 정의돼 있으며 각각의 이름은 stdin, stdout, stderr 이다.
    - (1): 표준 입력 스트림(standard input stream)
    - (2): 표준 출력 스트림(standard output steam)
    - (3): 표준 오류 스트림(standard error stream)
    - (4): stdio.h
*/


#include <stdio.h>


int main(void) {

    return 0;
}