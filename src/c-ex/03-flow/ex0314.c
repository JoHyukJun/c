#include <stdio.h>


int main(void)
{
    int n1, n2, diff;

    puts("input 2 number.");
    printf("n1 : ");
    scanf("%d", &n1);
    printf("n2 : ");
    scanf("%d", &n2);

    if (a == b)
        puts("eql");
    else {
        int min, max;

        if (a < b) {
            min = a;
            max = b;
        }
        else {
            min = b;
            max = a;
        }
    }

    printf("min: %d \n", min);
    printf("max: %d \n", max);
        
    return 0;
}