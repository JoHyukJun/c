/*
    - a + b * c 에서는 왼쪽의 덧셈보다 오른쪽의 곱셈이 먼저 수행된다.
    - 이는 연산자 * 의 (1) 가 연사자 + 의 그것보다 높기 때문이다.
    - 같은 (1) 의 연산자가 연속할 때 좌우 어느 쪽 연산을 우선할 것인가는 연산자의 (2) 에 따라 결정된다.
    - (1): 우선순위
    - (2): 결합성
*/


#include <stdio.h>


int main(void) {
    return 0;
}